# 逐根 K 线阅读模板

> **状态：Training / Derived**
>
> 本模板从当前[执行手册](../README.md#权威层级)派生，不替代其中的交易计划、订单/成交和管理合同边界。

## 记录原则

- 入场前只保留一份当前计划；条件改变时先撤单，确认旧订单已无活动数量后再重写计划并记录时间。
- 实际成交后不覆盖原始计划；每次订单变化、fill 和实际触发的管理事件按时间追加。
- 按时间追加不授权扩大原合同；只允许执行预写目标/runner、在预写事件成立时缩短目标或退出、按预写 trailing 推进 stop，以及按异常规则降险。
- 没有目标、失效 / stop、trailing、时间或异常事件时，默认持有，不重复填写持仓状态或重新计算。
- 部分成交时同时记录实际持仓和活动余单。
- Stop 的结构锚点、订单触发价和预计成交价分开；目标的结构区域、保守退出价和路径障碍分开。
- 只记录会实质改变判断的关键结构证据；gap、measured move、PT2、runner、TBTL 和 scale-in 均为条件字段，不是每笔交易的必经项目。

## 记录模板

```text
[基本信息]
日期 / 时间：
品种 / 方向 / 周期 / 时段：
账户货币 / point value / tick size / 最小交易单位：
stop buffer / target fill allowance / entry-stop-target 滑点假设：
本时段权限：

[市场与候选]
M 基础状态 / 叠加阶段：
关键结构证据（如有；例如大周期边界、相关 gap 的边界及接受 / 拒绝状态）：
K 候选 / 方向：
C 控制权、位置、PT1 空间 / 对手方最合理的理由及失败证据：
本根新增信息 / 多头理由 / 空头理由：
T 当前触发 / 触发后谁会入场、退出或止损，主要订单压力：

[交易计划 D]
订单方式 / 触发价或预计成交区 / 最差可接受成交价：
命题失效 / 容许的正常测试：
止损方案 / 结构锚点 / 首个成交起即生效的初始 stop 触发价 / 最差预计止损成交价：
MaxLoss / 超限处置：
初始数量；预设加仓层级、触发和数量：
PlannedLoss / 固定成本 / 随数量变化且未嵌入价格的成本：
路径障碍（按距离排序）：
PT1 依据 / 目标区 / 保守退出价 / 数量：
PT2 或 runner（如使用）的依据 / 目标区或 trailing / 数量：
若 measured move 被用作目标依据：类型 / 测量端点 / 投射起点 / 计算值：
G；若使用 TBTL，附着批次 / trailing：
订单到期 / 最晚入场：
提前退出或目标调整事件 / 最晚持仓或时段退出：

[Trader's Equation Q]
互斥结果（概率 / 毛账户盈亏 / 该路径未嵌入成本 / 净结果）：
毛价格盈利 / 毛价格亏损 / 预期全路径成本 / 入场期望：
敏感性检查 / 结论：

[订单与成交；每次变化追加]
时间 / 用途：
状态（允许下单、已挂单、部分成交、全部成交、撤销、到期、拒绝）：
计划数量 / 活动数量 / 本次及累计成交数量：
成交价 / 手续费 / 滑点 / 是否越过最差可接受成交价及处置：
每次入场 / 加仓成交后的实际总仓位 / protective stop 是否已覆盖 / 若 stop 成交的最差总结果：

[管理事件；只有实际触发时追加]
时间 / 事件（目标、失效 / stop、trailing、时间、异常）：
触发的原计划规则 / 执行动作：
事件前实际数量 / active stop / 剩余目标：
订单状态 / 本次成交数量与价格：
事件后实际数量 / active stop / 剩余目标：
```

退出订单未实际成交前，实际数量不能按计划预先减少。

实际 fill 越过最差可接受成交价时，取消全部未成交入场/加仓余单，恢复完整保护，并按真实 fill 检查最差总结果；它始终记为执行异常。成交后不得拉远固定目标、补加 runner / TBTL、升级 G 或加入未预设 scale-in。

## 趋势日模板

开盘后出现强趋势 K 线并有跟进时，初步判断可能形成趋势日。若随后回调浅、反方 K 线弱、顺势再次触发，则趋势日判断增强。

如果突破后没有跟进、回调变深并进入重叠，应降低趋势日判断。

## 交易区间模板

开盘上下波动、K 线重叠、收盘位置混杂时，先按方向不清处理。突破早期高低点后若缺乏跟进并回到区间内，追随者可能被困。

中部不追随普通信号，等待边缘、失败或清晰突破。

## 反转尝试模板

趋势成熟后，不因 climactic behavior 直接逆势。先等趋势线突破、反向强度、回探原极值失败，再评估反转 setup。

反转后若没有跟进，先按 trading range 处理，而不是坚持大反转。
